Renders one Lucide icon (CDN substitution — see readme Iconography) tinted via CSS mask so it inherits `currentColor` and adapts to theme automatically.

```jsx
<Icon name="refresh-cw" size={18}/>
```

Use kebab-case Lucide names (lucide.dev/icons). Always pair with an accessible label on the parent control — the icon itself is `aria-hidden`.
