Square icon-only button (44px default, meets mobile touch target minimum).

```jsx
<IconButton label="Refresh tile"><RefreshIcon/></IconButton>
```

Pass any icon glyph (see assets/icons) as children. `variant="secondary"` adds a border+surface fill for use outside a toolbar context.
