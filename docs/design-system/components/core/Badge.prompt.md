Small status pill — tile states, token status, invite status, connection status.

```jsx
<Badge tone="success">Active</Badge>
```

Tones: `neutral` (default, informational), `success`, `warning`, `danger`, `accent`. Never used for counts — use plain text for numbers.
