Hover/focus label — mainly for icon-only controls and keyboard-shortcut hints in Notes.

```jsx
<Tooltip label="Refresh (⌘R)"><IconButton label="Refresh"><Icon name="refresh-cw"/></IconButton></Tooltip>
```
