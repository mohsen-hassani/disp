Labeled text/number/email input — the base rendering for a JSON-Schema `string`/`number` field.

```jsx
<TextField label="Display name" value={name} onChange={setName} helpText="Shown in the top bar"/>
```

Pass `error` to switch the border/help text to danger tone.
