import js from '@eslint/js';
import jsxA11y from 'eslint-plugin-jsx-a11y';
import globals from 'globals';
import tseslint from 'typescript-eslint';

export default tseslint.config(
  {
    // src/api/generated is regenerated by `pnpm api:generate` (M02) and is
    // read-only from the implementer's side (WEB-SPEC §6.2); routeTree.gen.ts
    // is generated by TanStack Router (M04). TypeScript still type-checks
    // both — see tsconfig.json's `include`.
    ignores: ['dist', 'src/api/generated', 'src/routeTree.gen.ts', 'coverage', 'playwright-report'],
  },
  js.configs.recommended,
  ...tseslint.configs.recommended,
  {
    files: ['src/**/*.{ts,tsx}', 'tests/**/*.{ts,tsx}', 'public/**/*.js'],
    languageOptions: {
      ecmaVersion: 2022,
      globals: globals.browser,
    },
  },
  {
    // Node-side tooling config files run outside the browser.
    files: ['*.config.{ts,js}'],
    languageOptions: {
      globals: globals.node,
    },
  },
  {
    files: ['**/*.{jsx,tsx}'],
    ...jsxA11y.flatConfigs.recommended,
  },
);
